---
import { getEntry } from 'astro:content';
import SvelteGenerator from '~components/SvelteGenerator.svelte';

export type Props = {
	id: string;
};

const { id } = Astro.props;

const generator = await getEntry('generators', id);

if (generator === undefined) {
	throw new Error('Генератор не найден');
}

const data = generator.data;
---

<SvelteGenerator {data} client:visible />
